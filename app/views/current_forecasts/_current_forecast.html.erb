<%= turbo_frame_tag current_forecast do %>
  <div class="row">
    <div class="col s12 center">
      <h4><%=current_forecast.location.name%></h4>
      <i class="btn-floating btn-large waves-effect waves-light circle"><%=current_forecast.temp.to_f.round%> °F</i>
      <h5><%=current_forecast.status%></h5>
      <img src="<%= image_path(current_forecast.status_img_path) %>" />
    </div>
  </div>
<%end %>